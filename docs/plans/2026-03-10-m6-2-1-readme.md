# M6.2.1 README Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Complete `M6.2.1` by rewriting the root README into a practical entrypoint for understanding, running, and contributing to Portex.

**Architecture:** Keep the README layered: project framing first, quick start second, contributor commands third, and boundaries/docs last. Reuse current repository commands and milestone evidence instead of inventing new installation or deployment stories.

**Tech Stack:** Markdown, current repository commands, existing docs in `docs/`

---

### Task 1: Rewrite the README structure

**Files:**
- Modify: `README.md`
- Reference: `docs/progress.md`
- Reference: `docs/TODO.md`
- Reference: `AGENTS.md`

**Step 1: Write the failing check**

Define the missing sections to add:

```markdown
## Current Status
## Features
## Quick Start
## Development
## Project Structure
## Architecture
## Current Boundaries
## Documents
## Upstream Reference
```

The current README only contains a placeholder title and one sentence, so it does not satisfy `M6.2.1`.

**Step 2: Write minimal implementation**

Rewrite `README.md` using the approved structure and current repository facts only.

### Task 2: Verify commands and wording against the live repo

**Files:**
- Modify: `README.md` as needed

**Step 1: Check command fidelity**

Ensure every command in the README matches the current repository:
- backend install
- DB init
- backend run
- frontend install/run
- tests and lint
- provider sanity check

**Step 2: Check boundary wording**

Ensure the README:
- does not overstate Docker/runtime maturity
- does not overstate IM readiness
- does not claim remote CI execution evidence

### Task 3: Run verification and update handoff

**Files:**
- Modify: `docs/progress.md`
- Modify: `tasks/todo.md`

**Step 1: Run documentation-focused verification**

Run:
- `.venv/bin/pytest -o addopts='' -q`
- `.venv/bin/ruff check .`
- `cd web && npm run lint`
- `cd web && npm run build`

Expected: PASS

**Step 2: Record `M6.2.1`**

Document:
- README rewrite completed
- exact verification evidence
- next starting point after `M6.2.1`

### Task 4: Commit

**Files:**
- Commit all approved `M6.2.1` documentation updates

**Step 1: Commit**

Prepare a focused commit such as:
- `docs(readme): complete M6.2.1 project readme`
