# M6.1.3 CI/CD Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Complete `M6.1.3` by adding a minimal GitHub Actions workflow for backend and frontend verification, plus any missing dependency required by the chosen CI commands.

**Architecture:** Keep CI intentionally small: one workflow file with a backend job and a frontend job. Reuse the commands already proven locally instead of inventing new tooling, and add `pytest-cov` only because the TODO-specified backend command requires it.

**Tech Stack:** GitHub Actions, Python 3.11, pytest, pytest-cov, ruff, Node/npm

---

### Task 1: Add backend CI support

**Files:**
- Modify: `pyproject.toml`
- Create: `.github/workflows/test.yml`

**Step 1: Write the failing verification**

Confirm the current gap:

Run:
- `.venv/bin/pytest --help | rg -- '--cov'`
- `.venv/bin/python -m pip show pytest-cov`

Expected:
- `--cov` support is missing
- `pytest-cov` is not installed

**Step 2: Write minimal implementation**

- Add `pytest-cov` to `[project.optional-dependencies].dev`
- Create the backend job in `.github/workflows/test.yml` with:
  - checkout
  - Python 3.11 setup
  - dependency install
  - `pytest tests/ -v --cov`
  - `ruff check .`

**Step 3: Run verification to confirm backend commands work**

Run:
- `.venv/bin/pip install -e ".[dev]"`
- `.venv/bin/pytest tests/ -v --cov`
- `.venv/bin/ruff check .`

Expected: PASS

### Task 2: Add frontend CI support

**Files:**
- Modify: `.github/workflows/test.yml`
- Reference: `web/package.json`

**Step 1: Write the failing verification**

Use the workflow design requirement as the gap:
- frontend CI job does not exist yet

**Step 2: Write minimal implementation**

Extend `.github/workflows/test.yml` with a frontend job that:
- checks out code
- sets up Node
- runs `npm ci` in `web/`
- runs `npm run lint`
- runs `npm run build`

**Step 3: Run verification to confirm frontend commands work**

Run:
- `npm ci`
- `npm run lint`
- `npm run build`

Workdir: `web`

Expected: PASS

### Task 3: Update handoff docs and commit

**Files:**
- Modify: `docs/progress.md`
- Modify: `docs/TODO.md`
- Modify: `tasks/todo.md`

**Step 1: Record `M6.1.3`**

Document:
- workflow file added
- `pytest-cov` dependency added
- local workflow-equivalent verification evidence
- next start point `M6.2.1`
- note that the workflow has been configured locally but not executed on GitHub in this environment

**Step 2: Commit**

Prepare a focused commit such as:
- `build(ci): complete M6.1.3 workflow setup`
