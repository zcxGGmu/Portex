# M6.2.2 API Docs Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Complete `M6.2.2` by turning the existing FastAPI-generated `/docs` into a useful HTTP API reference for the current Portex routes.

**Architecture:** Keep FastAPI OpenAPI generation as the source of truth. Improve app metadata, route metadata, and schema metadata only; do not add a parallel Markdown API manual or try to fold the WebSocket contract into OpenAPI.

**Tech Stack:** FastAPI, Pydantic v2, pytest

---

### Task 1: Lock the OpenAPI documentation expectations with tests

**Files:**
- Modify: `tests/app/routes/test_api_routes.py`
- Reference: `app/main.py`
- Reference: `app/routes/auth.py`
- Reference: `app/routes/groups.py`
- Reference: `app/routes/messages.py`
- Reference: `app/routes/tasks.py`
- Reference: `domain/schemas.py`

**Step 1: Write the failing test**

Add focused tests for `/openapi.json` that assert:
- app-level description and tags exist
- `/tasks` documents the current error responses and UTC semantics
- `/messages` documents its current queued-placeholder behavior
- `RegisterRequest` and `CreateTaskRequest` fields expose descriptions/examples
- `DELETE /groups/{group_id}/members/{user_id}` has a concrete response schema

**Step 2: Run test to verify it fails**

Run: `.venv/bin/pytest tests/app/routes/test_api_routes.py -q`

Expected: FAIL because the current OpenAPI schema lacks those metadata fields.

### Task 2: Enrich the generated OpenAPI schema

**Files:**
- Modify: `app/main.py`
- Modify: `app/routes/auth.py`
- Modify: `app/routes/users.py`
- Modify: `app/routes/groups.py`
- Modify: `app/routes/messages.py`
- Modify: `app/routes/tasks.py`
- Modify: `domain/schemas.py`

**Step 1: Write minimal implementation**

- add app description and tag metadata in `app/main.py`
- add route `summary`, `description`, and `responses` metadata without changing runtime behavior
- add schema `description` and `examples` where the generated docs are currently ambiguous
- add an explicit delete response DTO for group-member removal

**Step 2: Run test to verify it passes**

Run: `.venv/bin/pytest tests/app/routes/test_api_routes.py -q`

Expected: PASS

### Task 3: Run milestone verification and update handoff

**Files:**
- Modify: `docs/progress.md`
- Modify: `tasks/todo.md`

**Step 1: Run focused and full verification**

Run:
- `.venv/bin/pytest tests/app/routes/test_api_routes.py -q`
- `.venv/bin/pytest -o addopts='' -q`
- `.venv/bin/ruff check .`
- `cd web && npm run lint`
- `cd web && npm run build`

Expected: PASS

**Step 2: Update restart-oriented docs**

Record:
- `M6.2.2` completion summary
- exact verification evidence
- current boundary note that WebSocket is still outside OpenAPI
- next starting point `M6.2.3`

### Task 4: Commit the milestone

**Files:**
- Commit all approved `M6.2.2` changes

**Step 1: Commit**

Prepare a focused commit such as:
- `docs(api): complete M6.2.2 api docs`
