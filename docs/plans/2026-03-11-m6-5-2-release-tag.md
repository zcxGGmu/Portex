# M6.5.2 Release Tag Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Complete `M6.5.2` by updating milestone handoff docs, creating the annotated release tag `v1.0.0`, and verifying the local/remote tag state without starting `M6.5.3` artifact-building work.

**Architecture:** Treat this milestone as a release-execution checkpoint around git metadata and restart-oriented documentation. Update the repository to reflect that `M6.5.2` is complete and `M6.5.3` is next, commit those changes, then create `v1.0.0` on the final milestone commit and verify the resulting tag state.

**Tech Stack:** Markdown, git, pytest, Ruff, npm

---

### Task 1: Record the phase boundary and session tracking

**Files:**
- Create: `docs/plans/2026-03-11-m6-5-2-release-tag-design.md`
- Create: `docs/plans/2026-03-11-m6-5-2-release-tag.md`
- Modify: `tasks/todo.md`

**Step 1: Write the design and plan docs**

Record:
- `M6.5.2` is tag execution, not version-string sync
- `M6.5.3` remains the artifact-building milestone
- local and remote `v1.0.0` state must be checked before push

**Step 2: Record the session checklist**

Add a new `tasks/todo.md` entry covering:
- preflight git/tag checks
- repo doc updates
- verification commands
- commit/tag/push steps

### Task 2: Update restart-oriented docs for post-tag state

**Files:**
- Modify: `README.md`
- Modify: `AGENTS.md`
- Modify: `docs/progress.md`
- Modify: `tasks/todo.md`

**Step 1: Update repo-facing docs**

Record:
- `M6.5.2` is complete
- next starting point is `M6.5.3`
- release tag `v1.0.0` exists or is being created in this milestone
- runtime/package version strings remain `0.1.0`

**Step 2: Sanity-check consistency**

Run:
- `rg -n "M6\\.5\\.2|M6\\.5\\.3|v1\\.0\\.0|0\\.1\\.0" README.md AGENTS.md docs/progress.md docs/plans/2026-03-11-m6-5-2-release-tag*`

Expected: the repo consistently describes `M6.5.2` as release-tag execution and `M6.5.3` as the next step.

### Task 3: Run milestone verification and create the release commit

**Files:**
- Verify repository state only; no product-code changes expected

**Step 1: Run verification**

Run:
- `git status --short --branch`
- `git rev-parse --verify refs/tags/v1.0.0`
- `git ls-remote --tags origin v1.0.0`
- `git diff --check`
- `.venv/bin/pytest -o addopts='' -q`
- `.venv/bin/ruff check .`
- `cd web && npm run lint`
- `cd web && npm run build`

Expected:
- `refs/tags/v1.0.0` is missing before creation
- remote `origin` has no `v1.0.0`
- repo verification commands pass

**Step 2: Commit the milestone**

Prepare a focused commit such as:
- `docs(release): complete M6.5.2 release tag`

### Task 4: Create and verify the tag

**Files:**
- Create local git ref: `refs/tags/v1.0.0`

**Step 1: Create the annotated tag**

Run:
- `git tag -a v1.0.0 -m "Release v1.0.0"`

Expected: local tag `v1.0.0` points to the `M6.5.2` completion commit.

**Step 2: Verify the local tag**

Run:
- `git show --stat v1.0.0`
- `git tag -n | rg "^v1\\.0\\.0"`

Expected: the tag exists locally with the correct annotation.

### Task 5: Push and verify the remote tag

**Files:**
- Update remote ref on `origin`: `refs/tags/v1.0.0`
- Modify: `docs/progress.md`
- Modify: `tasks/todo.md`

**Step 1: Push and verify**

Run:
- `git push origin v1.0.0`
- `git ls-remote --tags origin v1.0.0 v1.0.0^{}`

Expected: the remote now exposes both the annotated tag object and dereferenced commit.

**Step 2: Record final evidence**

Update:
- exact local tag verification evidence
- exact remote tag verification evidence
- the next starting point after `M6.5.2`
