# M6.3.1 Database Indexes Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Complete `M6.3.1` by adding the three TODO-defined database indexes and proving they are created during fresh SQLite initialization.

**Architecture:** Declare the indexes on the existing SQLAlchemy models so schema intent stays co-located with the tables. Verify them at two levels: model metadata and a temporary SQLite database initialized through the repository's real `scripts/init_db.py` entrypoint.

**Tech Stack:** SQLAlchemy 2.0, SQLite, pytest

---

### Task 1: Lock the index contract with failing tests

**Files:**
- Modify: `tests/domain/models/test_models.py`
- Modify: `tests/scripts/test_init_db.py`
- Reference: `domain/models/message.py`
- Reference: `domain/models/task.py`

**Step 1: Write the failing test**

Add:
- a metadata-level test asserting `Message.__table__.indexes` includes:
  - `idx_messages_chat_jid`
  - `idx_messages_timestamp`
- a metadata-level test asserting `ScheduledTask.__table__.indexes` includes:
  - `idx_tasks_next_run`
- a SQLite init test that runs `scripts/init_db.py` with a temporary database URL and confirms those index names exist in `sqlite_master`

**Step 2: Run test to verify it fails**

Run:
- `.venv/bin/pytest tests/domain/models/test_models.py tests/scripts/test_init_db.py -q`

Expected: FAIL because the current models do not define those indexes yet.

### Task 2: Add the minimal model indexes

**Files:**
- Modify: `domain/models/message.py`
- Modify: `domain/models/task.py`

**Step 1: Write minimal implementation**

Add SQLAlchemy index declarations using explicit names:

```python
__table_args__ = (
    Index("idx_messages_chat_jid", "chat_jid"),
    Index("idx_messages_timestamp", "timestamp"),
)
```

and:

```python
__table_args__ = (
    Index("idx_tasks_next_run", "next_run"),
)
```

**Step 2: Run test to verify it passes**

Run:
- `.venv/bin/pytest tests/domain/models/test_models.py tests/scripts/test_init_db.py -q`

Expected: PASS

### Task 3: Run milestone verification and update handoff

**Files:**
- Modify: `docs/progress.md`
- Modify: `tasks/todo.md`

**Step 1: Run verification**

Run:
- `.venv/bin/pytest tests/domain/models/test_models.py tests/scripts/test_init_db.py -q`
- `.venv/bin/pytest -o addopts='' -q`
- `.venv/bin/ruff check .`
- `cd web && npm run lint`
- `cd web && npm run build`

Expected: PASS

**Step 2: Update restart-oriented docs**

Record:
- `M6.3.1` completion summary
- exact verification evidence
- next starting point after `M6.3.1`

### Task 4: Commit the milestone

**Files:**
- Commit all approved `M6.3.1` changes

**Step 1: Commit**

Prepare a focused commit such as:
- `perf(db): complete M6.3.1 indexes`
