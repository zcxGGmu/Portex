# M4.1.1 User Model Extension Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Complete `M4.1.1` by extending the user model with profile and account state fields, while keeping the current auth/register/login/`/users/me` flow backward-compatible for already completed `M1`~`M3` capabilities.

**Architecture:** Treat `domain/models/user.py` as the storage contract, `services/auth.py` as the current in-memory public-user source, and `domain/schemas.py` / `app/routes/users.py` as the API contract surface. Add the new fields consistently across these layers with safe defaults so the current auth middleware and route tests keep working without introducing partial user states.

**Tech Stack:** Python 3.11, SQLAlchemy ORM, Pydantic, FastAPI, pytest

---

### Task 1: Define the extended user contract with failing tests

**Files:**
- Modify: `tests/domain/models/test_models.py`
- Modify: `tests/services/test_auth_service.py`
- Modify: `tests/app/routes/test_api_routes.py`
- Reference: `domain/models/user.py`
- Reference: `services/auth.py`
- Reference: `domain/schemas.py`

**Step 1: Write failing tests**

Cover:
- SQLAlchemy `User` model exposing the new columns from `docs/TODO.md`
- `AuthService.register_user()` returning the new user attributes with safe defaults
- `/users/me` serializing the newly added fields without breaking existing auth flow

**Step 2: Run focused tests to verify RED**

Run: `.venv/bin/pytest tests/domain/models/test_models.py tests/services/test_auth_service.py tests/app/routes/test_api_routes.py -q`

Expected: FAIL because the current model/schema/auth surface does not expose the new fields yet.

### Task 2: Implement the extended model and public schema

**Files:**
- Modify: `domain/models/user.py`
- Modify: `domain/schemas.py`
- Modify: `services/auth.py`

**Step 1: Extend the persistence model**

Add nullable profile/account metadata fields plus `must_change_password` to the SQLAlchemy model with explicit defaults.

**Step 2: Extend the current auth user shape**

Update the in-memory `AuthUser` dataclass and registration flow so the public user contract matches the extended model contract.

**Step 3: Keep API responses aligned**

Update `UserResponse` so `/users/me` returns the expanded user payload while preserving the existing route behavior.

**Step 4: Run focused tests to verify GREEN**

Run: `.venv/bin/pytest tests/domain/models/test_models.py tests/services/test_auth_service.py tests/app/routes/test_api_routes.py -q`

Expected: PASS.

### Task 3: Regressions, docs, and commit

**Files:**
- Modify: `docs/TODO.md`
- Modify: `docs/progress.md`

**Step 1: Run verification**

Run:
- `.venv/bin/pytest tests/domain/models/test_models.py tests/services/test_auth_service.py tests/app/routes/test_api_routes.py -q`
- `.venv/bin/pytest -q`
- `.venv/bin/ruff check .`
- `cd web && npm run lint`
- `cd web && npm run build`

**Step 2: Update docs**

Mark `M4.1.1` complete in `docs/TODO.md`, record exact verification evidence in `docs/progress.md`, and advance the next start point to `M4.1.2`.

**Step 3: Commit**

Commit with a focused `feat(user): ...` message after the workspace is cleanly verified.
